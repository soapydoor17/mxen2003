Part A psuedo code
If range sensor forward > some distance: {
  move forward
  If range sensor left > range sensor right:{
    move left until equal}
  elIf range sensor right > range sensor left:{
  move right until equal}
  }

cmVal_left = (7000/rsVal_left) - 6;
cmVal_right = (7000/rsVal_right) - 6;
cmVal_front = (7000/rsVal_front) - 6;
Part A code
if (cmVal_front > 8cm) {
  

Part B
If range sensor forward < some distance: {
  Stop move forward
  If range sensor left > range sensor right: {
    rotate left 90*}
  elif range sensor right > range sensor left: {
    rotate right 90*}
  }

Part C
If range sensor forward < some smaller distance: {
  move backwards}

     
